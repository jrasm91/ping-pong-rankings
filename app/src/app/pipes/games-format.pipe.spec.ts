import { GamesFormatPipe } from './games-format.pipe';

describe('GamesFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new GamesFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
