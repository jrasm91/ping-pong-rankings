import { SetsFormatPipe } from './sets-format.pipe';

describe('SetsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SetsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
